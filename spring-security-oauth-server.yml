server:
  port: 8081

app.message: This message form the 'spring-security-oauth-server.yml' file


eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aouth_security?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    dialect: org.hibernate.dialect.MySQLDialect
    username: root
    password: admin

hibernate.dialect: hibernate.dialect
