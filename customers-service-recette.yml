app.message: This message for the 'customers-service-recette.yml' file

spring:
  profiles: recette
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
datasource:
  driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://localhost:3306/petclinic_prod?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
  dialect: org.hibernate.dialect.MySQLDialect
  username: root
  password: admin

hibernate:
  dialect: hibernate.dialect
